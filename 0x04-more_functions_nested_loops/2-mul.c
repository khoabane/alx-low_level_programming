#include "main.h"

/**
* mul -  a function that multiplies two integers.
*@a: a variable to multiply
*@b: a variable to multiply
*Return: returns a product of a and b.
*/

int mul(int a, int b)
{
int product;
product = a * b;
return (product);
}

